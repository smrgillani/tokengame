<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gstokengame">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/Home_ABT" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/Register_ABT" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".UserSettingsActivity"
            android:label="@string/User_ABT" />
        <activity
            android:name=".TokensActivity"
            android:label="@string/Tokens_ABT" />
        <activity
            android:name=".TokenActivity"
            android:label="@string/Token_ABT"/>
        <activity android:name=".ChangepassActivity" android:label="@string/Password_ABT"></activity>
    </application>

</manifest>